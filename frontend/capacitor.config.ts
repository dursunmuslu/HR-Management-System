import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dursunmuslu.orvia',
  appName: 'Orvia HR',
  webDir: 'dist/hr-management-frontend/browser'
};

export default config;
